---
import { formatDate } from "../../script/utils";
import type { CollectionEntry } from "astro:content";

interface Props {
  posts: CollectionEntry<"blog">[];
}

const { posts } = Astro.props as Props;
---

<div class="loc fixedContent">
  <div class="comp mntl-taxonomysc-article-list-group mntl-block">
    <div
      class="comp l-container mntl-taxonomysc-article-list mntl-document-card-list mntl-card-list mntl-block page-width"
    >
      {
        [...posts, ...posts].map((post) => (
          <a
            href={`/blog/${post.slug}`}
            class="comp mntl-card-list-items mntl-document-card mntl-card card card--no-image"
            data-cta="Read Now ▸"
          >
            <div class="loc card__top">
              <div class="card__media mntl-universal-image universal-image__container">
                <div class="img-placeholder" style="padding-bottom:100.0%;">
                  <img
                    src={post.data.image.src}
                    alt={post.data.image.alt}
                    class="card__img universal-image__image lazyloaded"
                  />
                </div>
              </div>
            </div>
            <div class="card__content">
              <div class="card__header" />
              <span class="card__title">
                <span class="card__title-text ">{post.data.title}</span>
              </span>
              <div
                class="comp card__meta mntl-block"
                data-category={post.data.category}
                data-updated={formatDate(post.data.date)}
              >
                <span class="comp card__square-divider mntl-block" />
              </div>
            </div>
          </a>
        ))
      }
    </div>
  </div>
</div>

<style>
  /* --------------- */
  /* General         */
  /* --------------- */

  div,
  span {
    margin: 0;
    padding: 0;
    font-size: 100%;
    font-weight: 400;
    vertical-align: baseline;
    background: 0 0;
  }

  img {
    max-width: 100%;
    vertical-align: bottom;
  }

  a {
    margin: 0;
    padding: 0;
    font-size: 100%;
    vertical-align: baseline;
    background: 0 0;
  }

  a {
    color: #222;
    text-decoration: none;
  }

  /* --------------- */
  /* Component       */
  /* --------------- */

  /* div class="loc fixedContent" */

  /* div class="comp mntl-taxonomysc-article-list-group mntl-block" */
  .mntl-taxonomysc-article-list-group {
    position: relative;
  }

  /* div class="comp l-container mntl-taxonomysc-article-list mntl-document-card-list mntl-card-list mntl-block" */
  .l-container {
    width: 100%;
    margin: 0 auto;
  }

  .l-container {
    max-width: 75rem;
    padding: 0 1.5rem;
  }

  .mntl-taxonomysc-article-list {
    grid-gap: 1.375rem;
  }

  .mntl-card-list {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  @supports (display: grid) {
    .mntl-card-list {
      display: grid;
      grid-template-columns: 1fr;
      grid-gap: 1rem;
    }
  }

  @media (min-width: 35em) {
    .mntl-card-list {
      flex-direction: row;
      flex-wrap: wrap;
    }
  }

  @media (min-width: 35em) {
    @supports (display: grid) {
      .mntl-card-list {
        grid-template-columns: repeat(2, 1fr);
      }
    }
  }

  @media (min-width: 54em) {
    @supports (display: grid) {
      .mntl-card-list {
        grid-template-columns: repeat(3, 1fr);
      }
    }
  }

  @media (min-width: 65em) {
    @supports (display: grid) {
      .mntl-card-list {
        grid-template-columns: repeat(4, 1fr);
      }
    }
  }

  .mntl-taxonomysc-article-list.mntl-card-list {
    gap: 1.5rem;
  }

  .mntl-taxonomysc-article-list.mntl-card-list {
    gap: 1.25rem 1.25rem;
    padding-top: 2.5rem;
  }

  .mntl-taxonomysc-article-list.mntl-card-list:first-child {
    margin-bottom: 0;
  }

  /* a class="comp mntl-card-list-items mntl-document-card mntl-card card card--no-image" */
  .card {
    flex: 1 0 100%;
    position: relative;
  }

  .card {
    display: flex;
    flex-grow: 1;
    flex-direction: column;
    padding: 0.7rem 1rem;
    overflow: hidden;
    width: 100%;
    background: #fff;
    cursor: pointer;
    font-weight: 700;
  }

  .mntl-card-list .card {
    margin-bottom: 1rem;
    padding: 0;
  }
  @supports (display: grid) {
    .mntl-card-list .card {
      margin-bottom: 0;
    }
  }

  @media (min-width: 35em) {
    .mntl-card-list .card {
      flex: 0 1 48%;
    }
  }

  @media (min-width: 54em) {
    .mntl-card-list .card {
      flex: 0 1 32%;
    }
  }

  /* div class="loc card__top" */

  /* div class="card__media mntl-universal-image universal-image__container" */
  .card__media {
    overflow: hidden;
    position: relative;
  }

  .card__media {
    height: 100%;
    margin-bottom: 1rem;
    order: 1;
    position: relative;
  }
  .mntl-card .card__media {
    margin-bottom: 0;
    height: auto;
  }

  /* div class="img-placeholder" */
  .img-placeholder {
    background-color: #c7c7c7;
    height: 0;
    overflow: hidden;
  }

  .img-placeholder {
    background-color: #c7c7c7;
    position: relative;
    width: 100%;
  }

  .img-placeholder.img-placeholder {
    background: #eee;
  }

  .card .img-placeholder {
    position: relative;
  }

  .card__media .img-placeholder {
    position: relative;
  }
  /* img class="card__img universal-image__image lazyloaded" */
  .card__img {
    width: 100%;
    height: auto;
    max-width: none;
    display: block;
  }

  .card .card__img {
    transition: transform 0.15s ease;
    transition-property: transform;
  }

  .card__media .img-placeholder img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    -o-object-fit: cover;
    object-fit: cover;
  }

  .card__media .img-placeholder img {
    height: 100%;
    width: 100%;
    position: absolute;
    -o-object-fit: cover;
    object-fit: cover;
    -o-object-position: center top;
    object-position: center top;
  }

  .card:hover .img-placeholder img {
    transform: scale(1.05);
  }

  /* div class="card__content" */
  .card__content {
    padding: 1rem;
    line-height: 1.3;
  }

  .card__content {
    flex-grow: 1;
    order: 2;
    width: 100%;
  }

  .mntl-card .card__content {
    display: flex;
    flex-direction: column;
  }

  .mntl-taxonomysc-article-list-group .card__content {
    background-color: #fbf9f6;
  }

  @media (min-width: 34em) {
    .mntl-taxonomysc-article-list-group .card__content {
      background-color: #fff;
    }
  }

  /* ::after */
  .card::after {
    position: absolute;
    bottom: -0.5rem;
    left: 50%;
    width: 7.5rem;
    height: 1px;
    transform: translateX(-50%);
    background: #ede6e6;
  }

  .card[data-cta]:not([data-cta=""])::after {
    display: block;
    order: 99;
    margin-top: 1rem;
    content: attr(data-cta);
  }
  /* div class="card__header" */

  /* span class="card__title" */
  .card__title {
    letter-spacing: 0.5px;
    font-size: 1.25rem;
    color: #222;
    display: inline;
    word-break: break-word;
    overflow-wrap: anywhere;
  }

  .card__title {
    box-sizing: border-box;
    margin-bottom: 0;
    font-size: 1.125rem;
    font-weight: 700;
    line-height: 1.2em;
  }

  .mntl-card .card__title {
    order: 2;
  }

  .mntl-taxonomysc-article-list-group .card__title {
    font-size: 1rem;
  }

  @media (min-width: 34em) {
    .mntl-taxonomysc-article-list-group .card__title {
      margin-bottom: 1rem;
      font-size: 1.0625rem;
      line-height: 24px;
    }
  }

  /* span class="card__title-text " */
  @media (min-width: 50em) {
    .mntl-taxonomysc-article-list-group .card__title-text {
      font-size: 1.5rem;
      line-height: 26px;
    }
  }

  .mntl-card:hover .card__title-text {
    background-size: 2px 2px;
    background-repeat: repeat-x;
    background-position: 0 100%;
    background-image: linear-gradient(
      rgb(var(--color-base-accent)),
      rgb(var(--color-base-accent))
    );
  }

  /* div class="comp card__meta mntl-block" */
  .card__meta {
    margin-bottom: 0.5rem;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-size: 0.6875rem;
    line-height: 1.5;
  }

  /* ::before */
  /* ::after */
  .card__meta::before {
    content: attr(data-category);
  }

  .card__meta::after {
    content: attr(data-updated);
    display: inline-block;
  }
  .card__meta::after,
  .card__meta::before {
    letter-spacing: 0.04rem;
  }

  .mntl-taxonomysc-article-list-group .card__meta::after,
  .mntl-taxonomysc-article-list-group .card__meta::before {
    color: #444;
    font-size: 0.625rem;
  }

  @media (min-width: 34em) {
    .mntl-taxonomysc-article-list-group .card__meta::after,
    .mntl-taxonomysc-article-list-group .card__meta::before {
      font-size: 0.6875rem;
    }
  }

  /* span class="comp card__square-divider mntl-block" */
  .card__square-divider {
    background-color: transparent;
    display: inline-block;
    width: 5px;
    height: 5px;
    margin-left: 5px;
    margin-right: 10px;
    vertical-align: middle;
    transform: translateY(-0.1rem);
  }

  /* ::before */
  .card__square-divider::before {
    content: "•";
    transform: translateY(-1.15rem);
    display: block;
    font-size: 1.775rem;
    color: rgb(var(--color-base-accent));
  }
</style>
